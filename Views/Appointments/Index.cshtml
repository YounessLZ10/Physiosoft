@model IEnumerable<Physiosoft.Data.Appointment>

@{
    ViewData["Title"] = "Appointment Index";
}

<h1 class="mb-4">Appointment List</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>

<div asp-validation-summary="ModelOnly" class="text-danger"></div>

<div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-3">
    @foreach (var item in Model)
    {
        <div class="col mb-4">
            <div class="card h-100">
                <div class="card-body">
                    <p class="card-text">@Html.DisplayFor(modelItem => item.Patient.Lastname)</p>
                    <p class="card-text">@Html.DisplayFor(modelItem => item.Physio.Lastname)</p>
                    <h5 class="card-title">@Html.DisplayFor(modelItem => item.AppointmentDate)</h5>
                    <p class="card-text">@Html.DisplayFor(modelItem => item.DurationMinutes)</p>
                    <p class="card-text">@Html.DisplayFor(modelItem => item.AppointmentStatus)</p>
                  
                </div>
                <div class="card-footer">
                    <div class="btn-group" role="group" aria-label="Actions">
                        <a class="btn btn-info btn-sm" asp-action="Details" asp-route-id="@item.AppointmentID">Details</a>
                        <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@item.AppointmentID">Edit</a>
                        <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.AppointmentID">Delete</a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
